version: '3'

services:
#  db:
#    image: postgres
#    volumes:
#      - ./database:/var/lib/postgresql/data
#    #Uncomment to diagnose DB from host
#    #ports: "5432:5432"
  web:
    build: .
    command: python3 manage.py runserver 0.0.0.0:8000
    volumes:
      - "./application:/code"
    ports:
      - "8000:8000"
#    depends_on:
#      - db
#  visualizer:
#    image: dockersamples/visualizer:stable
#    ports:
#      - "8080:8080"
#    volumes:
#      - "/var/run/docker.sock:/var/run/docker.sock"
#    deploy:
#      placement:
#        constraints: [node.role == manager]
#    networks:
#      - webnet